# To'g'ridan-to'g'ri bog'liqliklar (tranzitiv paketlar avtomatik o'rnatiladi)
aiogram==3.31.0
python-dotenv==1.2.3

# GitHub'ga zaxiralash uchun (aiogram ham ishlatadi, lekin biz to'g'ridan-to'g'ri import qilamiz)
aiohttp==3.14.3

# Zaxirani shifrlash uchun (BACKUP_ENCRYPT_KEY berilganda)
cryptography==50.0.1

# Windows va ba'zi Linux konteynerlarida zoneinfo uchun vaqt zonasi bazasi kerak.
# Busiz ZoneInfo("Asia/Tashkent") ZoneInfoNotFoundError bilan yiqiladi.
tzdata==2026.4
